# Offline modeling pipeline (xG + prospect models). Pinned for reproducibility.
numpy==2.4.6
pandas==3.0.6
pyarrow==25.0.1
scikit-learn==1.9.1
scipy==1.17.1
xgboost-cpu==3.2.0  # CPU-only build: same API, no ~700 MB of GPU libraries
pytest==9.1.1
